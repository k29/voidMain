<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE symbol-and-basic-behavior-files SYSTEM "../symbol-and-basic-behavior-files.dtd">
<option xmlns="http://www.xabsl.de" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="checklocalize" initial-state="localize">
  &mySymbols;
  &myBasicBehaviors;
  &options;
  <state name="localize">
    <subsequent-basic-behavior ref="doLocalize"/>
    <decision-tree>
      <if>
        <condition description="Sufficient confidence and ball not found">
          <greater-than>
            <decimal-input-symbol-ref ref="confidence"/>
            <constant-ref ref="TH"/>
          </greater-than>
        </condition>
        <transition-to-state ref="findball"/>
      </if>
      <else>
        <transition-to-state ref="localize"/>
      </else>
    </decision-tree>
  </state>
  <state name="findball">
    <subsequent-basic-behavior ref="doFindBall"/>
    <subsequent-option ref="ballfound"/>
    <decision-tree>
      <if>
        <condition description="Insufficient confidence">
          <less-than>
            <decimal-input-symbol-ref ref="confidence"/>
            <constant-ref ref="TH"/>
          </less-than>
        </condition>
        <transition-to-state ref="localize"/>
      </if>
      <else>
        <transition-to-state ref="findball"/>
      </else>
    </decision-tree>
  </state>
</option>
