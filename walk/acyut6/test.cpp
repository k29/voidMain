#include <stdio.h>
#include "leg.h"
#include "communication.h"
#include <math.h>
#include "AcYut.h"

int main()
{
	Communication comm;
	//AcYut bot(&comm);
	
	//bot.left_leg->pingMotor(3);
	//bot.left_leg->ledonm(3);
	//bot.right_leg->GP(1);
	//bot.right_leg->GP(2);
	//bot.left_leg->GP(1);
	//bot.left_leg->GP(2);
	
	return 0;
}
